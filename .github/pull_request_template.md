## What this changes

<!-- What does this pull request do, and why? Link the issue it fixes, if there is one ("Fixes #123"). -->

## How it was tested

<!-- For example: "python -m pytest with QT_QPA_PLATFORM=offscreen", plus what you tried by hand on Windows. -->

## Checklist

- [ ] The tests pass (`python -m pytest` with `QT_QPA_PLATFORM=offscreen`).
- [ ] New behavior has a test, where that is practical.
- [ ] `CHANGELOG.md` has an entry under "Unreleased" for anything a user would notice.
- [ ] User-facing text is in US English.
